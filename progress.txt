# Ralph Progress Log
Started: Fri Mar 20 17:28:59 KST 2026
---

## Codebase Patterns
- maplibre-gl v5: import as `import maplibregl, { type Map } from 'maplibre-gl'`, use `new maplibregl.Map({...})`
- maplibre-gl mock in vitest: use `class` syntax (not arrow function) in `vi.fn().mockImplementation`; return `{ default: { Map: MockClass } }`
- maplibre-gl CSS: `import 'maplibre-gl/dist/maplibre-gl.css'` — mock in tests with `vi.mock('maplibre-gl/dist/maplibre-gl.css', () => ({}))`
- CharacterSystem and HotspotLayer temporarily removed from App.tsx — will be re-added in V4-3/V4-4 with geo-based coordinates

## 2026-03-22 - V4-1
- Installed maplibre-gl v5.21.0
- Created src/components/MapView.tsx — MapLibre GL JS map centered on Seoul using demotiles style
- Removed src/components/SeoulMap.tsx (SVG-based map)
- Updated App.tsx to use MapView; removed CharacterSystem/HotspotLayer (re-added later in V4-3/V4-4)
- Replaced SeoulMap.test.tsx with MapView.test.tsx
- Updated App.test.tsx to mock maplibre-gl and check for map-container
- All 69 tests pass, build succeeds
- **Learnings for future iterations:**
  - vitest mock for maplibre-gl must use `class` syntax for Map constructor, not arrow function
  - maplibre-gl CSS import also needs to be mocked in test files
  - App.tsx temporarily has no hotspot markers or character system — these come in V4-3/V4-4
---
